parameters:
- name: licensed
  type: boolean
  default: false
- name: verbose
  type: boolean
  default: false

steps:
- checkout: 'self'
  clean: true
  fetchTags: false
  fetchDepth: 1

- task: NodeTool@0
  inputs:
    versionSpec: '20.x'
  displayName: 'Install Node.js'

- task: Npm@1
  displayName: 'npm ci'
  inputs:
    command: 'ci'
    verbose: ${{ parameters.verbose }}

# Static gates first: none of them need a build, so a lint, type or manifest error
# fails in seconds instead of after the full build + validate cycle.
- task: Npm@1
  displayName: 'Lint'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run lint'

- task: Npm@1
  displayName: 'Type-check build scripts'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run typecheck:scripts'

- task: Npm@1
  displayName: 'Check publish manifests'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run check:manifests'

- task: Npm@1
  displayName: 'Build'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run build'
  env:
    ${{ if eq(parameters.licensed, true) }}: # only set if param is true
      IG_LICENSED_BUILD:  ${{ parameters.licensed }}


- task: Npm@1
  displayName: 'Validate dist packages'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run validate:dist'

- script: npx playwright install chromium-headless-shell
  displayName: 'Install Playwright browsers'

- task: Npm@1
  displayName: 'Test'
  inputs:
    command: 'custom'
    verbose: ${{ parameters.verbose }}
    customCommand: 'run test:ci'
  env:
      CI: 'true'
